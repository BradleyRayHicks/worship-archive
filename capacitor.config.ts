import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.worshiparchive.app',
  appName: 'Worship Archive',
  webDir: 'build'
};

export default config;
